{
  "name": "scooter-media-site",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "preview": "python3 -m http.server 8000 --directory dist",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.130.0"
  }
}
